/* This wrapper pulls specific functions we need from the SheetJS library */
import { utils, read, writeFileXLSX } from 'xlsx';

// Export only the functions we need
export { utils, read, writeFileXLSX };
