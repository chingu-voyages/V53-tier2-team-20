// Allergy types
export interface Allergy {
    id: string;
    name: string;
    createdAt: Date;
}

// Menu types
export interface MenuItem {
    id: string;
    name: string;
    createdAt: Date;
}
